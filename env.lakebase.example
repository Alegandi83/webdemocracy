# ============================================================================
# Web Democracy - Databricks Lakebase Configuration
# ============================================================================
# Copia questo file in .env.lakebase e compila con le tue credenziali
#
# IMPORTANTE: Databricks Lakebase è PostgreSQL-compatibile (porta 5432)
# NON usare SQL Warehouse (porta 443) - quello è un servizio diverso!

# ============================================================================
# DATABRICKS LAKEBASE CONNECTION (PostgreSQL-compatible)
# ============================================================================

# Hostname del tuo Lakebase instance
# Formato: <instance-name>.cloud.databricks.com o IP diretto
LAKEBASE_HOST=your-lakebase-instance.cloud.databricks.com

# Porta (standard PostgreSQL)
LAKEBASE_PORT=5432

# Nome del database
LAKEBASE_DATABASE=default

# Nome dello schema (default: webdemocracy)
# In PostgreSQL, lo schema è un namespace all'interno del database
# Web Democracy usa lo schema 'webdemocracy' per isolare le sue tabelle
LAKEBASE_SCHEMA=webdemocracy

# Credenziali di accesso
LAKEBASE_USER=your_username
LAKEBASE_PASSWORD=your_password

# SSL Mode (per connessioni sicure)
# Opzioni: disable, allow, prefer, require, verify-ca, verify-full
LAKEBASE_SSLMODE=require

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Attiva modalità Lakebase
USE_LAKEBASE=true

# ============================================================================
# COME OTTENERE LE CREDENZIALI
# ============================================================================
#
# 1. Vai al tuo workspace Databricks
# 2. Naviga su "Data" > "Lakebase"
# 3. Seleziona il tuo database instance
# 4. Nella sezione "Connection Details" troverai:
#    - Hostname (LAKEBASE_HOST)
#    - Port (5432)
#    - Database name (LAKEBASE_DATABASE)
# 5. Le credenziali (user/password) le hai impostate durante
#    la creazione del Lakebase instance
# 6. Schema: Web Democracy usa "webdemocracy" come schema dedicato
#    per isolare le tabelle dall'ambiente condiviso
#
# NOTA: Se usi IP whitelisting, assicurati che il tuo IP sia autorizzato
#
# ESEMPI DI CONFIGURAZIONE:
#
# Configurazione standard (raccomandato):
#   LAKEBASE_DATABASE=default
#   LAKEBASE_SCHEMA=webdemocracy
#
# Configurazione multi-ambiente:
#   LAKEBASE_DATABASE=production_db
#   LAKEBASE_SCHEMA=webdemocracy_prod
#
# Multi-tenant con schemi separati:
#   LAKEBASE_DATABASE=shared_db
#   LAKEBASE_SCHEMA=webdemocracy_tenant_123

